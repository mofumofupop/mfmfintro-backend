{
  "name": "mfmfintro-backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "format": "prettier --write ./src",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.87.1",
    "hono": "^4.10.7"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "wrangler": "^4.4.0"
  }
}
